## 1. 数据库变更

- [ ] 1.1 创建 `user_favorites` 表迁移脚本，包含字段 `(id, user_id, article_id, created_at)`
- [ ] 1.2 在 `(user_id, article_id)` 上添加唯一约束
- [ ] 1.3 在 `article_id` 上添加外键级联删除约束
- [ ] 1.4 执行迁移并验证表结构正确

## 2. 后端 API — 收藏操作

- [ ] 2.1 实现 `POST /articles/:id/favorite` 收藏文章接口，需鉴权
- [ ] 2.2 实现 `DELETE /articles/:id/favorite` 取消收藏接口，需鉴权
- [ ] 2.3 处理重复收藏的幂等逻辑（唯一约束冲突时返回 `200 OK`）
- [ ] 2.4 处理取消不存在收藏的幂等逻辑（记录不存在时返回 `200 OK`）
- [ ] 2.5 未登录用户调用收藏接口时统一返回 `401`

## 3. 后端 API — 收藏列表

- [ ] 3.1 实现 `GET /users/me/favorites` 获取当前用户收藏列表接口，需鉴权
- [ ] 3.2 添加分页支持，限制单页最大返回 50 条

## 4. 后端 — 收藏状态注入

- [ ] 4.1 在文章列表接口中，对已登录用户通过 `LEFT JOIN user_favorites` 附加 `is_favorited` 字段
- [ ] 4.2 在文章详情接口中，对已登录用户同样注入 `is_favorited` 字段
- [ ] 4.3 未登录用户访问列表和详情时，`is_favorited` 始终返回 `false`

## 5. 前端 — 收藏按钮

- [ ] 5.1 实现收藏按钮组件，支持已收藏/未收藏两种状态样式
- [ ] 5.2 在文章列表页中集成收藏按钮，根据 `is_favorited` 字段渲染初始状态
- [ ] 5.3 在文章详情页中集成收藏按钮
- [ ] 5.4 实现收藏/取消收藏点击事件，调用对应 API 并即时更新按钮状态
- [ ] 5.5 未登录用户点击收藏按钮时拦截操作并引导跳转登录页

## 6. 前端 — 我的收藏页

- [ ] 6.1 创建"我的收藏"列表页路由与页面组件
- [ ] 6.2 接入 `GET /users/me/favorites` 接口，展示收藏文章列表
- [ ] 6.3 实现分页加载功能
- [ ] 6.4 在收藏列表中支持取消收藏操作，操作后从列表移除对应条目

## 7. 测试与验收

- [ ] 7.1 后端单元测试：收藏/取消收藏接口的幂等性
- [ ] 7.2 后端单元测试：未登录请求返回 401
- [ ] 7.3 后端单元测试：文章删除后收藏记录级联清理
- [ ] 7.4 前端集成测试：收藏按钮状态切换流程
- [ ] 7.5 端到端验收：完整覆盖四条验收标准